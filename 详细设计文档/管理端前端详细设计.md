# 快递代取与驿站管理系统——管理端前端详细设计

> 本文档为《快递代取与驿站管理系统详细设计文档》的管理端前端实现详细设计，基于 **PureAdmin** (Vue3 + Element Plus) 框架。管理端供驿站管理员和系统管理员使用，实现包裹管理、出库核销、货架管理、用户管理、数据统计等核心业务功能。

---

## 目录

- [1. 总体架构设计](#1-总体架构设计)
- [2. 技术栈与依赖](#2-技术栈与依赖)
- [3. 项目目录结构](#3-项目目录结构)
- [4. 路由与菜单设计](#4-路由与菜单设计)
- [5. 页面模块详细设计](#5-页面模块详细设计)
- [6. 公共组件设计](#6-公共组件设计)
- [7. 状态管理设计](#7-状态管理设计)
- [8. API 请求层设计](#8-api-请求层设计)
- [9. 权限控制设计](#9-权限控制设计)
- [10. 原型设计说明](#10-原型设计说明)

---

## 1. 总体架构设计

### 1.1 架构概览

```
┌─────────────────────────────────────────────────────────┐
│                    PureAdmin 管理端                      │
├─────────────┬─────────────┬─────────────┬───────────────┤
│   视图层     │   状态层     │   路由层     │   请求层      │
│  (Vue3 SFC) │  (Pinia)    │  (vue-router)│  (axios)     │
├─────────────┴─────────────┴─────────────┴───────────────┤
│              PureAdmin 基础框架层                        │
│  (权限控制 / 布局 / 主题 / 国际化 / 指令 / 工具函数)      │
├─────────────────────────────────────────────────────────┤
│                    HTTP / Axios                         │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                   后端 API 网关                          │
│                  /api/v1/*                               │
└─────────────────────────────────────────────────────────┘
```

### 1.2 PureAdmin 框架集成

PureAdmin 是基于 Vue3 + TypeScript + Vite + Element Plus 的后台管理框架，提供：

| 能力         | 说明                                         |
| ------------ | -------------------------------------------- |
| **权限控制** | RBAC 权限模型，支持菜单权限、按钮权限、路由守卫 |
| **布局系统** | 多种布局模式（左侧菜单、顶部菜单、混合菜单）   |
| **主题系统** | 亮色/暗色主题，自定义主题色                   |
| **国际化**   | 中英文切换                                     |
| **请求封装** | 基于 Axios 的请求拦截、响应拦截、错误统一处理   |
| **组件库**   | Element Plus 二次封装，表格、表单、弹窗等常用组件 |
| **状态管理** | Pinia 状态管理，支持持久化                     |

---

## 2. 技术栈与依赖

### 2.1 核心依赖

| 类别       | 技术                     | 版本    | 用途                   |
| ---------- | ------------------------ | ------- | ---------------------- |
| 框架       | Vue 3                    | ^3.4    | 视图层                 |
| 构建工具   | Vite                     | ^5.x    | 开发/构建              |
| 语言       | TypeScript               | ^5.x    | 类型安全               |
| UI库       | Element Plus             | ^2.7    | 组件库                 |
| 管理框架   | @pureadmin/core          | 最新版  | PureAdmin 核心         |
| 状态管理   | Pinia                    | ^2.x    | 全局状态               |
| 路由       | vue-router               | ^4.x    | 路由管理               |
| HTTP请求   | Axios                    | ^1.x    | API请求                |
| 图表       | ECharts                  | ^5.x    | 数据可视化             |
| 代码规范   | ESLint + Prettier        | -       | 代码风格统一           |

### 2.2 开发依赖

| 类别     | 技术                | 用途                 |
| -------- | ------------------- | -------------------- |
| 类型声明 | @types/*            | TypeScript 类型      |
| 测试     | Vitest + Vue Test Utils | 单元测试          |
| Mock     | vite-plugin-mock    | 接口Mock             |

---

## 3. 项目目录结构

基于 PureAdmin 实际项目结构调整：

```
admin/
├── public/                          # 静态资源
├── src/
│   ├── api/                         # API 接口定义（按模块组织）
│   │   ├── auth.ts                  #   认证模块
│   │   ├── parcel.ts                #   包裹模块
│   │   ├── pickup.ts                #   取件核销模块
│   │   ├── proxy.ts                 #   代取模块
│   │   ├── shelf.ts                 #   货架模块
│   │   ├── stats.ts                 #   统计模块
│   │   ├── station.ts               #   驿站模块
│   │   └── user.ts                  #   用户模块
│   ├── assets/                      # 静态资源
│   │   ├── icons/                   #   图标
│   │   └── styles/                  #   样式
│   ├── components/                  # 公共组件
│   │   ├── RePureTableBar/          #   PureAdmin 表格工具栏（已内置）
│   │   ├── ReIcon/                  #   PureAdmin 图标组件（已内置）
│   │   ├── ReDialog/                #   PureAdmin 弹窗组件（已内置）
│   │   ├── ParcelStatusTag/         #   包裹状态标签
│   │   ├── ShelfSelector/           #   货架选择器
│   │   └── CourierCompanySelect/    #   快递公司选择器
│   ├── config/                      # 全局配置
│   │   └── index.ts
│   ├── directives/                  # 指令
│   │   └── permission.ts            #   权限指令
│   ├── layout/                      # 布局组件（PureAdmin内置）
│   │   ├── index.vue
│   │   ├── components/
│   │   └── hooks/
│   ├── plugins/                     # 插件
│   │   └── i18n/                    #   国际化
│   ├── router/                      # 路由配置
│   │   ├── index.ts                 #   路由实例
│   │   ├── enums.ts                 #   路由排序枚举
│   │   ├── utils.ts                 #   路由工具函数
│   │   └── modules/                 #   路由模块（每个文件对应一个菜单）
│   │       ├── home.ts              #     工作台
│   │       ├── parcel.ts            #     包裹管理
│   │       ├── pickup.ts            #     出库核销
│   │       ├── shelf.ts             #     货架管理
│   │       ├── proxy.ts             #     代取管理
│   │       ├── user.ts              #     用户管理
│   │       ├── stats.ts             #     数据统计
│   │       ├── station.ts           #     驿站管理
│   │       └── remaining.ts         #     登录等静态路由
│   ├── store/                       # Pinia 状态管理
│   │   ├── index.ts
│   │   ├── types.ts
│   │   ├── utils.ts
│   │   └── modules/
│   │       ├── user.ts              #   用户状态
│   │       ├── app.ts               #   应用状态
│   │       ├── permission.ts        #   权限状态
│   │       ├── multiTags.ts         #   标签页状态
│   │       └── epTheme.ts           #   主题状态
│   ├── utils/                       # 工具函数
│   │   ├── auth.ts                  #   认证工具
│   │   ├── http/                    #   HTTP 请求封装
│   │   │   └── index.ts
│   │   ├── format/                  #   格式化函数
│   │   │   ├── phone.ts             #     手机号脱敏
│   │   │   ├── datetime.ts          #     日期时间格式化
│   │   │   └── money.ts             #     金额格式化
│   │   └── validate/                #   校验函数
│   ├── views/                       # 页面视图（按模块组织）
│   │   ├── login/                   #   登录页
│   │   │   ├── index.vue
│   │   │   └── components/
│   │   ├── welcome/                 #   工作台（PureAdmin内置）
│   │   │   └── index.vue
│   │   ├── parcel/                  #   包裹管理
│   │   │   ├── inbound/             #     入库管理
│   │   │   │   └── index.vue
│   │   │   ├── list/                #     包裹列表
│   │   │   │   ├── index.vue
│   │   │   │   ├── data.ts
│   │   │   │   └── utils/
│   │   │   └── batch-import/        #     批量导入
│   │   │       └── index.vue
│   │   ├── pickup/                  #   出库核销
│   │   │   ├── index.vue
│   │   │   ├── data.ts
│   │   │   └── utils/
│   │   ├── shelf/                   #   货架管理
│   │   │   ├── index.vue
│   │   │   ├── data.ts
│   │   │   └── utils/
│   │   ├── proxy/                   #   代取订单
│   │   │   ├── index.vue
│   │   │   ├── data.ts
│   │   │   └── utils/
│   │   ├── user/                    #   用户管理
│   │   │   ├── list/
│   │   │   │   ├── index.vue
│   │   │   │   └── data.ts
│   │   │   └── runner/
│   │   │       ├── audit/
│   │   │       │   ├── index.vue
│   │   │       │   └── data.ts
│   │   │       └── utils/
│   │   ├── stats/                   #   数据统计
│   │   │   ├── overview/
│   │   │   │   └── index.vue
│   │   │   ├── courier/
│   │   │   │   └── index.vue
│   │   │   └── finance/
│   │   │       └── index.vue
│   │   └── station/                 #   驿站管理
│   │       ├── index.vue
│   │       ├── data.ts
│   │       └── utils/
│   ├── style/                       # 全局样式
│   │   └── index.scss
│   ├── App.vue
│   └── main.ts                      # 入口文件
├── types/                           # 全局类型声明
│   ├── global.d.ts
│   ├── router.d.ts
│   └── components.d.ts
├── build/                           # Vite 构建配置
├── .env                             # 环境变量
├── .env.development                 # 开发环境
├── .env.production                  # 生产环境
├── index.html                       # 入口HTML
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

## 4. 路由与菜单设计

### 4.1 路由排序枚举

```typescript
// src/router/enums.ts
// 完整版菜单比较多，将 rank 抽离出来，在此方便维护

const home = 0,        // 平台规定只有 home 路由的 rank 才能为 0
  parcel = 1,          // 包裹管理
  pickup = 2,          // 出库核销
  shelf = 3,           // 货架管理
  proxy = 4,           // 代取管理
  user = 5,            // 用户管理
  stats = 6,           // 数据统计
  station = 7;         // 驿站管理

export {
  home,
  parcel,
  pickup,
  shelf,
  proxy,
  user,
  stats,
  station
};
```

### 4.2 路由模块示例

#### 工作台路由

```typescript
// src/router/modules/home.ts
import { $t } from "@/plugins/i18n";
import { home } from "@/router/enums";

const { VITE_HIDE_HOME } = import.meta.env;
const Layout = () => import("@/layout/index.vue");

export default {
  path: "/",
  name: "Home",
  component: Layout,
  redirect: "/welcome",
  meta: {
    icon: "ep/home-filled",
    title: $t("menus.pureHome"),
    rank: home
  },
  children: [
    {
      path: "/welcome",
      name: "Welcome",
      component: () => import("@/views/welcome/index.vue"),
      meta: {
        title: $t("menus.pureHome"),
        showLink: VITE_HIDE_HOME === "true" ? false : true
      }
    }
  ]
} satisfies RouteConfigsTable;
```

#### 包裹管理路由

```typescript
// src/router/modules/parcel.ts
import { $t } from "@/plugins/i18n";
import { parcel } from "@/router/enums";

export default {
  path: "/parcel",
  redirect: "/parcel/list",
  meta: {
    icon: "ep/box",
    title: "包裹管理",
    rank: parcel
  },
  children: [
    {
      path: "/parcel/inbound",
      name: "ParcelInbound",
      component: () => import("@/views/parcel/inbound/index.vue"),
      meta: {
        title: "包裹入库",
        icon: "ep/upload"
      }
    },
    {
      path: "/parcel/list",
      name: "ParcelList",
      component: () => import("@/views/parcel/list/index.vue"),
      meta: {
        title: "包裹列表",
        icon: "ep/list"
      }
    },
    {
      path: "/parcel/batch-import",
      name: "ParcelBatchImport",
      component: () => import("@/views/parcel/batch-import/index.vue"),
      meta: {
        title: "批量导入",
        icon: "ep/upload-filled"
      }
    }
  ]
} satisfies RouteConfigsTable;
```

### 4.3 完整路由配置

| 路由文件        | 路径前缀       | 菜单标题   | 图标            | 排序 | 角色权限     |
| --------------- | -------------- | ---------- | --------------- | ---- | ------------ |
| `home.ts`       | `/`            | 工作台     | ep/home-filled  | 0    | 所有管理员   |
| `parcel.ts`     | `/parcel`      | 包裹管理   | ep/box          | 1    | 所有管理员   |
| `pickup.ts`     | `/pickup`      | 出库核销   | ep/check        | 2    | 所有管理员   |
| `shelf.ts`      | `/shelf`       | 货架管理   | ep/grid         | 3    | 所有管理员   |
| `proxy.ts`      | `/proxy`       | 代取订单   | ep/van          | 4    | 所有管理员   |
| `user.ts`       | `/user`        | 用户管理   | ep/user         | 5    | 所有管理员   |
| `stats.ts`      | `/stats`       | 数据统计   | ep/pie-chart    | 6    | 所有管理员   |
| `station.ts`    | `/station`     | 驿站管理   | ep/office-building | 7 | super_admin |

---

## 5. 页面模块详细设计

> 详细原型设计请参见：[原型设计/README.md](./原型设计/README.md)

### 5.1 登录页

**文件位置**：`src/views/login/index.vue`

**组件使用**：
- `el-form` + `el-form-item`：登录表单
- `el-input`：用户名、密码输入框
- `el-checkbox`：记住密码
- `el-button`：登录按钮

**功能说明**：管理员通过用户名+密码登录，支持记住密码。

**数据结构**：

```typescript
// 请求
interface LoginRequest {
  username: string;
  password: string;
}

// 响应
interface LoginResponse {
  code: number;
  message: string;
  data: {
    accessToken: string;
    refreshToken: string;
    expires: Date;
    roles: string[];
    permissions: string[];
  };
}
```

---

### 5.2 工作台/仪表盘

**文件位置**：`src/views/welcome/index.vue`（复用PureAdmin内置welcome页面，或自定义）

**组件使用**：
- `el-row` + `el-col`：栅格布局
- `el-card`：数据卡片
- `pure-table`：数据表格
- ECharts：趋势图表

**功能说明**：展示今日运营数据概览、快捷操作入口。

**API调用**：`GET /api/v1/stats/dashboard`

**数据结构**：

```typescript
interface DashboardData {
  date: string;
  station_id: number;
  today_inbound: number;
  today_outbound: number;
  pending_count: number;
  delayed_count: number;
  abnormal_count: number;
  proxy_active: number;
  shelf_usage_rate: number;
}
```

---

### 5.3 包裹入库管理

**文件位置**：`src/views/parcel/inbound/index.vue`

**组件使用**：
- `el-radio-group`：入库模式切换（扫码/手动）
- `el-form` + `el-form-item`：入库表单
- `el-input`：快递单号、收件人手机号等输入框
- `el-select`：快递公司下拉选择
- `el-checkbox-group`：特殊标签（易碎、生鲜等）
- `el-button`：确认入库、清空表单
- `pure-table`：入库成功记录列表

**功能说明**：管理员通过扫码或手动输入方式完成包裹入库。

**API调用**：`POST /api/v1/parcels/scan-in`

**数据结构**：

```typescript
// 入库请求
interface ScanInRequest {
  tracking_no: string;
  courier_company: string;
  receiver_phone: string;
  receiver_name?: string;
  shelf_code?: string;
  weight?: number;
  is_fragile?: boolean;
  remarks?: string;
}

// 入库响应
interface ScanInResponse {
  parcel_id: number;
  pickup_code: string;
  shelf_code: string;
  storage_time: string;
}
```

---

### 5.4 包裹列表管理

**文件位置**：`src/views/parcel/list/index.vue`

**组件使用**：
- `el-form`：筛选表单
- `el-input`：搜索输入框
- `el-select`：状态下拉、快递公司下拉
- `el-date-picker`：入库时间范围
- `PureTableBar`：表格工具栏（含新增、刷新、列设置按钮）
- `pure-table`：包裹列表表格
- `el-pagination`：分页组件
- `el-tag`：状态标签（使用ParcelStatusTag组件）
- `el-dropdown`：操作列下拉菜单

**功能说明**：查看所有包裹信息，支持多条件筛选、状态变更、查看详情。

**API调用**：`GET /api/v1/parcels`

**筛选参数**：

| 参数              | 类型    | 说明                           |
| ----------------- | ------- | ------------------------------ |
| `tracking_no`     | string  | 快递单号模糊匹配               |
| `receiver_phone`  | string  | 收件人手机号                   |
| `status`          | number  | 状态：1-待取 2-已取 3-滞留 4-已退 5-异常 |
| `courier_company` | string  | 快递公司                       |
| `shelf_code`      | string  | 货架编号                       |
| `storage_start`   | string  | 入库开始时间                   |
| `storage_end`     | string  | 入库结束时间                   |
| `page`            | number  | 页码                           |
| `page_size`       | number  | 每页条数                       |

---

### 5.5 出库核销管理

**文件位置**：`src/views/pickup/index.vue`

**组件使用**：
- `el-radio-group`：核销方式切换（扫码/手动）
- `el-input`：取件码输入框
- `el-button`：确认核销按钮
- `pure-table`：最近核销记录表格

**功能说明**：管理员扫描取件码或手动输入完成包裹出库核销。

**API调用**：`POST /api/v1/pickup/verify`

**数据结构**：

```typescript
// 核销请求
interface PickupVerifyRequest {
  pickup_code: string;
  verification_method: 1 | 2;  // 1-扫码 2-手动输入
  station_id: number;
}

// 核销响应
interface PickupVerifyResponse {
  parcel_id: number;
  tracking_no: string;
  pickup_time: string;
  operator_type: number;
  proxy_order_id?: number;
}
```

---

### 5.6 货架管理

**文件位置**：`src/views/shelf/index.vue`

**组件使用**：
- `el-row` + `el-col`：左右分栏布局
- `el-card`：货架详情卡片
- `pure-table`：货架列表表格
- `el-button`：新增货架、编辑
- `el-tag`：占用率标签（带颜色）
- `el-dialog`：新增/编辑弹窗
- `el-form`：货架表单

**功能说明**：可视化管理货架布局，查看实时占用情况，支持增删改查。

**API调用**：
- 列表：`GET /api/v1/shelves`
- 新增：`POST /api/v1/shelves`
- 更新：`PUT /api/v1/shelves/{id}`
- 热力图：`GET /api/v1/shelves/occupancy`

**数据结构**：

```typescript
interface ShelfItem {
  id: number;
  station_id: number;
  shelf_code: string;
  row_num: number;
  col_num: number;
  current_capacity: number;
  max_capacity: number;
  occupancy_rate: number;
  remark: string;
}

interface ShelfFormRequest {
  station_id: number;
  shelf_code: string;
  row_num: number;
  col_num: number;
  max_capacity: number;
  remark?: string;
}
```

---

### 5.7 代取订单管理

**文件位置**：`src/views/proxy/index.vue`

**组件使用**：
- `el-form`：筛选表单
- `el-input`：搜索输入框
- `el-select`：状态下拉
- `el-date-picker`：时间范围
- `PureTableBar`：表格工具栏
- `pure-table`：订单列表表格
- `el-tag`：状态标签
- `el-pagination`：分页

**功能说明**：查看所有代取订单，监控订单状态流转，处理异常订单。

**API调用**：`GET /api/v1/proxy/my-orders`

**订单状态枚举**：

| 状态码 | 状态文案   | 说明                     |
| ------ | ---------- | ------------------------ |
| 1      | 待接单     | 等待跑腿员接单           |
| 2      | 配送中     | 跑腿员已接单，配送中     |
| 3      | 待确认     | 跑腿员已送达，等待确认   |
| 4      | 已完成     | 收件人确认收货           |
| 5      | 已取消     | 发布者取消               |
| 6      | 取件失败   | 取件过程异常             |

---

### 5.8 用户管理

**文件位置**：`src/views/user/list/index.vue`

**组件使用**：
- `el-form`：筛选表单
- `el-input`：搜索输入框
- `el-select`：用户类型下拉、黑名单筛选
- `PureTableBar`：表格工具栏
- `pure-table`：用户列表表格
- `el-button`：查看取件记录、加入/移出黑名单

**功能说明**：查看所有用户信息，管理黑名单，查看用户取件记录。

**API调用**：`GET /api/v1/users`

---

### 5.9 跑腿员审核

**文件位置**：`src/views/user/runner/audit/index.vue`

**组件使用**：
- `el-form`：筛选表单
- `el-select`：状态筛选
- `PureTableBar`：表格工具栏
- `pure-table`：申请列表表格
- `el-button`：审核按钮
- `el-dialog`：审核弹窗
- `el-image`：证件照预览

**功能说明**：审核跑腿员申请，查看申请人资料，批准或拒绝申请。

**API调用**：
- 列表：`GET /api/v1/user/runner/applications`
- 审核：`PUT /api/v1/user/runner/applications/{id}/audit`

**数据结构**：

```typescript
interface RunnerApplicationItem {
  id: number;
  user_id: number;
  real_name: string;
  phone: string;
  student_id: string;
  id_card_image: string;
  status: number;  // 1-审核中 2-通过 3-拒绝
  audit_remark: string;
  created_at: string;
  updated_at: string;
}

interface RunnerAuditRequest {
  action: "approve" | "reject";
  audit_remark?: string;
}
```

---

### 5.10 数据统计

**文件位置**：`src/views/stats/overview/index.vue`

**组件使用**：
- `el-radio-group`：时间范围切换
- `el-date-picker`：自定义时间范围
- `el-row` + `el-col`：栅格布局
- `el-card`：统计卡片
- ECharts：趋势图表、柱状图

**子页面**：
- 数据概览：`/stats/overview`
- 快递公司对账：`/stats/courier`
- 代取财务：`/stats/finance`

**API调用**：
- 趋势图：`GET /api/v1/stats/trend`
- 代取财务：`GET /api/v1/stats/proxy-finance`
- 快递对账：`GET /api/v1/stats/courier-check`

---

### 5.11 驿站管理（系统管理员）

**文件位置**：`src/views/station/index.vue`

**组件使用**：
- `el-form`：筛选表单
- `el-input`：搜索输入框
- `el-select`：状态筛选
- `PureTableBar`：表格工具栏
- `pure-table`：驿站列表表格
- `el-button`：新增驿站、编辑
- `el-dialog`：新增/编辑弹窗
- `el-form`：驿站表单

**功能说明**：系统管理员管理所有驿站信息，支持新增、编辑驿站。

**API调用**：
- 列表：`GET /api/v1/stations`
- 新增：`POST /api/v1/stations`
- 编辑：`PUT /api/v1/stations/{id}`

**数据结构**：

```typescript
interface StationFormRequest {
  name: string;
  address: string;
  latitude: number;
  longitude: number;
  business_hours?: string;
  status?: number;
}
```

---

## 6. 公共组件设计

### 6.1 PureAdmin 内置组件

以下组件已内置，直接使用：

| 组件               | 说明                             |
| ------------------ | -------------------------------- |
| `PureTableBar`     | 表格工具栏（含标题、按钮、列设置） |
| `ReIcon`           | 图标组件（支持图标选择器）       |
| `ReDialog`         | 弹窗组件（支持拖拽、全屏）       |
| `ReDrawer`         | 抽屉组件                         |
| `ReQrcode`         | 二维码生成组件                   |
| `ReCropper`        | 图片裁剪组件                     |

### 6.2 自定义组件

#### ParcelStatusTag

```vue
<!-- src/components/ParcelStatusTag/index.vue -->
<template>
  <el-tag :type="tagType" :effect="effect">{{ statusText }}</el-tag>
</template>

<script setup lang="ts">
interface Props {
  status: number;
  effect?: "dark" | "light" | "plain";
}

const props = withDefaults(defineProps<Props>(), {
  effect: "light"
});

const statusMap: Record<number, { text: string; type: string }> = {
  1: { text: "待取件", type: "primary" },
  2: { text: "已取件", type: "success" },
  3: { text: "滞留", type: "warning" },
  4: { text: "已退件", type: "info" },
  5: { text: "异常", type: "danger" }
};

const tagType = computed(() => statusMap[props.status]?.type ?? "info");
const statusText = computed(() => statusMap[props.status]?.text ?? "未知");
</script>
```

#### ShelfSelector

```vue
<!-- src/components/ShelfSelector/index.vue -->
<template>
  <el-select
    v-model="selectedValue"
    :placeholder="placeholder"
    :disabled="disabled"
    filterable
    @change="handleChange"
  >
    <el-option
      v-for="shelf in shelves"
      :key="shelf.id"
      :label="`${shelf.shelf_code} (${shelf.current_capacity}/${shelf.max_capacity})`"
      :value="shelf.shelf_code"
      :disabled="shelf.current_capacity >= shelf.max_capacity"
    />
  </el-select>
</template>

<script setup lang="ts">
import { ref, onMounted } from "vue";
import { getShelfList } from "@/api/shelf";

interface Props {
  modelValue?: string;
  stationId: number;
  placeholder?: string;
  disabled?: boolean;
}

const props = withDefaults(defineProps<Props>(), {
  placeholder: "选择货架",
  disabled: false
});

const emit = defineEmits<{
  "update:modelValue": [value: string];
  change: [value: string];
}>();

const selectedValue = computed({
  get: () => props.modelValue,
  set: (val) => emit("update:modelValue", val)
});

const shelves = ref<any[]>([]);

const loadShelves = async () => {
  const res = await getShelfList({ station_id: props.stationId });
  shelves.value = res.list;
};

const handleChange = (val: string) => {
  emit("change", val);
};

onMounted(() => {
  loadShelves();
});
</script>
```

---

## 7. 状态管理设计

### 7.1 Store 模块

| Store模块    | 文件位置              | 说明                         |
| ------------ | --------------------- | ---------------------------- |
| `user`       | `store/modules/user.ts`     | 认证状态（token、用户信息） |
| `app`        | `store/modules/app.ts`      | 应用状态（主题、侧边栏）   |
| `permission` | `store/modules/permission.ts` | 权限状态（菜单、路由）   |
| `multiTags`  | `store/modules/multiTags.ts`  | 标签页状态               |
| `epTheme`    | `store/modules/epTheme.ts`    | 主题状态                 |

### 7.2 user Store

```typescript
// src/store/modules/user.ts
import { defineStore } from "pinia";
import { store, router, resetRouter, routerArrays, storageLocal } from "../utils";
import { type UserResult, getLogin, refreshTokenApi } from "@/api/auth";
import { useMultiTagsStoreHook } from "./multiTags";
import { type DataInfo, setToken, removeToken, userKey } from "@/utils/auth";

export type userType = {
  avatar: string;
  username: string;
  nickname: string;
  roles: Array<string>;
  permissions: Array<string>;
  verifyCode: string;
  currentPage: number;
  isRemembered: boolean;
  loginDay: number;
};

export const useUserStore = defineStore("pure-user", {
  state: (): userType => ({
    avatar: storageLocal().getItem<DataInfo<number>>(userKey)?.avatar ?? "",
    username: storageLocal().getItem<DataInfo<number>>(userKey)?.username ?? "",
    nickname: storageLocal().getItem<DataInfo<number>>(userKey)?.nickname ?? "",
    roles: storageLocal().getItem<DataInfo<number>>(userKey)?.roles ?? [],
    permissions: storageLocal().getItem<DataInfo<number>>(userKey)?.permissions ?? [],
    verifyCode: "",
    currentPage: 0,
    isRemembered: false,
    loginDay: 7
  }),
  actions: {
    SET_AVATAR(avatar: string) {
      this.avatar = avatar;
    },
    SET_USERNAME(username: string) {
      this.username = username;
    },
    SET_NICKNAME(nickname: string) {
      this.nickname = nickname;
    },
    SET_ROLES(roles: Array<string>) {
      this.roles = roles;
    },
    SET_PERMS(permissions: Array<string>) {
      this.permissions = permissions;
    },
    async loginByUsername(data) {
      return new Promise<UserResult>((resolve, reject) => {
        getLogin(data)
          .then(data => {
            if (data.code === 0) {
              setToken(data.data);
              resolve(data);
            } else {
              reject(data.message);
            }
          })
          .catch(error => {
            reject(error);
          });
      });
    },
    logOut() {
      this.username = "";
      this.roles = [];
      this.permissions = [];
      removeToken();
      useMultiTagsStoreHook().handleTags("equal", [...routerArrays]);
      resetRouter();
      router.push("/login");
    },
    async handRefreshToken(data) {
      return new Promise<RefreshTokenResult>((resolve, reject) => {
        refreshTokenApi(data)
          .then(data => {
            if (data.code === 0) {
              setToken(data.data);
              resolve(data);
            } else {
              reject(data.message);
            }
          })
          .catch(error => {
            reject(error);
          });
      });
    }
  }
});

export function useUserStoreHook() {
  return useUserStore(store);
}
```

---

## 8. API 请求层设计

### 8.1 HTTP 请求封装

```typescript
// src/utils/http/index.ts
import axios from "axios";
import type { AxiosInstance, AxiosRequestConfig, AxiosResponse } from "axios";
import { ElMessage } from "element-plus";
import { getAccessToken, removeAccessToken } from "@/utils/auth";
import { router } from "@/router";

const http: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 15000,
  headers: {
    "Content-Type": "application/json; charset=utf-8"
  }
});

// 请求拦截器
http.interceptors.request.use(
  (config) => {
    const token = getAccessToken();
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    config.headers["X-Client-Type"] = "admin";
    config.headers["X-Client-Version"] = "1.0.0";
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器
http.interceptors.response.use(
  (response) => {
    const res = response.data;
    if (res.code !== 0) {
      ElMessage.error(res.msg || "请求失败");
      if (res.code === 10002) {
        removeAccessToken();
        router.push("/login");
      }
      return Promise.reject(new Error(res.msg));
    }
    return res.data;
  },
  (error) => {
    if (error.response?.status === 401) {
      removeAccessToken();
      router.push("/login");
    }
    ElMessage.error(error.message || "网络异常");
    return Promise.reject(error);
  }
);

export { http };
```

### 8.2 API 模块化组织

```typescript
// src/api/parcel.ts
import { http } from "@/utils/http";

export type ParcelItem = {
  id: number;
  tracking_no: string;
  courier_company: string;
  shelf_code: string;
  pickup_code: string;
  receiver_phone: string;
  receiver_name: string;
  status: number;
  status_text: string;
  storage_time: string;
  pickup_time: string | null;
  notify_count: number;
};

export type ScanInRequest = {
  tracking_no: string;
  courier_company: string;
  receiver_phone: string;
  receiver_name?: string;
  shelf_code?: string;
  weight?: number;
  is_fragile?: boolean;
  remarks?: string;
};

export type ScanInResponse = {
  parcel_id: number;
  pickup_code: string;
  shelf_code: string;
  storage_time: string;
};

/** 扫码/手动入库 */
export const scanIn = (data: ScanInRequest) => {
  return http.request<ScanInResponse>("post", "/parcels/scan-in", { data });
};

/** 包裹列表 */
export const getParcelList = (params?: object) => {
  return http.request<any>("get", "/parcels", { params });
};

/** 包裹详情 */
export const getParcelDetail = (id: number) => {
  return http.request<any>("get", `/parcels/${id}`);
};

/** 更改包裹状态 */
export const updateParcelStatus = (id: number, data: object) => {
  return http.request<any>("put", `/parcels/${id}/status`, { data });
};
```

---

## 9. 权限控制设计

### 9.1 角色定义

| 角色           | 标识          | 权限范围                     |
| -------------- | ------------- | ---------------------------- |
| 系统管理员     | `super_admin` | 所有功能，可管理多个驿站     |
| 驿站管理员     | `station_admin` | 所属驿站的所有功能         |
| 只读管理员     | `viewer`      | 仅查看，不可操作             |

### 9.2 权限指令

PureAdmin 内置 `v-permission` 指令：

```vue
<!-- 按钮级权限控制 -->
<el-button v-permission="['parcel:inbound']">包裹入库</el-button>
<el-button v-permission="['proxy:audit']">审核</el-button>
```

---

## 10. 原型设计说明

详细页面原型设计已创建为独立的HTML文件，位于 `详细设计文档/原型设计/` 目录下。

**原型设计列表**：

| 文件名              | 说明                 |
| ------------------- | -------------------- |
| `login.html`        | 登录页原型           |
| `dashboard.html`    | 工作台/仪表盘原型    |
| `parcel-inbound.html` | 包裹入库原型       |
| `parcel-list.html`  | 包裹列表原型         |
| `pickup.html`       | 出库核销原型         |
| `shelf.html`        | 货架管理原型         |
| `proxy.html`        | 代取订单原型         |
| `user.html`         | 用户管理原型         |
| `runner-audit.html` | 跑腿员审核原型       |
| `stats.html`        | 数据统计原型         |
| `station.html`      | 驿站管理原型         |

---

## 附录 A：API 接口对照表

管理端前端调用的全部 API 接口，与后端 API 详细设计文档一一对应：

| 模块   | 接口                         | 方法   | 说明               | 权限           |
| ------ | ---------------------------- | ------ | ------------------ | -------------- |
| 认证   | `/auth/login`                | POST   | 管理员登录         | 公开           |
|        | `/auth/refresh`              | POST   | 刷新Token          | 已登录         |
| 包裹   | `/parcels/scan-in`           | POST   | 扫码/手动入库      | 管理员         |
|        | `/parcels/batch-in`          | POST   | 批量导入           | 管理员         |
|        | `/parcels`                   | GET    | 包裹列表           | 管理员         |
|        | `/parcels/{id}`              | GET    | 包裹详情           | 管理员         |
|        | `/parcels/{id}/status`       | PUT    | 更改状态           | 管理员         |
| 取件   | `/pickup/verify`             | POST   | 核销取件           | 管理员         |
|        | `/pickup/logs`               | GET    | 取件日志           | 管理员         |
| 货架   | `/shelves`                   | GET    | 货架列表           | 管理员         |
|        | `/shelves`                   | POST   | 新增货架           | 管理员         |
|        | `/shelves/{id}`              | PUT    | 更新货架           | 管理员         |
|        | `/shelves/occupancy`         | GET    | 货架占用热力图     | 管理员         |
| 代取   | `/proxy/my-orders`           | GET    | 代取订单列表       | 管理员         |
| 用户   | `/users`                     | GET    | 用户列表           | 管理员         |
|        | `/user/runner/applications`  | GET    | 跑腿员申请列表     | 管理员         |
|        | `/user/runner/applications/{id}/audit` | PUT | 审核跑腿员    | 管理员         |
| 统计   | `/stats/dashboard`           | GET    | 首页看板           | 管理员         |
|        | `/stats/trend`               | GET    | 包裹趋势图         | 管理员         |
|        | `/stats/proxy-finance`       | GET    | 代取财务汇总       | 管理员         |
|        | `/stats/courier-check`       | GET    | 快递公司对账       | 管理员         |
| 驿站   | `/stations`                  | GET    | 驿站列表           | 系统管理员     |
|        | `/stations`                  | POST   | 新增驿站           | 系统管理员     |
|        | `/stations/{id}`             | PUT    | 编辑驿站           | 系统管理员     |

---

> 文档结束。管理端前端设计严格遵循 API 详细设计文档的接口规范，确保前后端联调一致性。
