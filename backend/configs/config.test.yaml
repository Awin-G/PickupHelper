server:
  port: 8081
  read_timeout: 15s
  write_timeout: 15s

mysql:
  host: 127.0.0.1
  port: 3306
  username: root
  password: "1973"
  database: pickup_helper_test
  max_open_conns: 10
  max_idle_conns: 5
  conn_max_lifetime: 60s

redis:
  host: 127.0.0.1
  port: 6379
  password: ""
  db: 1
  pool_size: 10

jwt:
  access_secret: "test-access-secret"
  refresh_secret: "test-refresh-secret"
  access_ttl: 1h
  refresh_ttl: 24h
  issuer: pickup-helper-test

log:
  level: info
  format: json

rate_limit:
  qps: 1000
  burst: 2000

cors:
  allowed_origins:
    - "*"
